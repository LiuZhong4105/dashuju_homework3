# Hive 存储格式测评 - 快速开始指南

## 项目简介

这是一个完整的 Hive 存储格式性能测评项目，用于完成大数据课程作业3。本项目提供从零开始的完整配置和测试流程。

## 测试目标

1. 搭建 Hadoop + Hive 环境
2. 使用 TPC-H 基准测试工具生成测试数据
3. 对比至少 2 种存储格式（本项目支持 4 种）
4. 执行至少 5 条 SQL 查询进行性能测试
5. 分析和对比存储空间及查询性能

## 支持的存储格式

- **TextFile** - 文本格式（基准对照）
- **ORC** - 优化行列式存储（推荐用于 Hive）
- **Parquet** - 列式存储（推荐用于跨平台）
- **RCFile** - 行列混合存储（遗留格式）

## 系统要求

- Ubuntu 24.04 全新安装
- 至少 10GB 可用磁盘空间
- sudo 权限
- 网络连接

## 五步完成测试

### 第1步：安装依赖 (5分钟)

```bash
cd setup
sudo bash 01-install-prerequisites.sh
```

### 第2步：安装 Hadoop (10分钟)

```bash
bash 02-install-hadoop.sh
source ~/.bashrc
hdfs namenode -format
start-dfs.sh
start-yarn.sh
jps  # 验证服务运行
```

### 第3步：安装 Hive (5分钟)

```bash
bash 03-install-hive.sh
source ~/.bashrc
hdfs dfs -mkdir -p /user/hive/warehouse /tmp/hive
hdfs dfs -chmod g+w /user/hive/warehouse
hdfs dfs -chmod 777 /tmp/hive
schematool -initSchema -dbType derby
```

### 第4步：准备测试数据生成器 (3分钟)

```bash
bash 04-setup-testbench.sh
```

### 第5步：运行测试 (30-60分钟，取决于数据规模)

```bash
cd ../scripts

# 生成 1GB 测试数据
bash generate-data.sh 1

# 创建不同格式的表
bash create-tables.sh

# 运行基准测试查询
bash run-queries.sh

# 查看对比结果
bash compare-results.sh
```

## 测试流程图

```
[安装依赖] → [安装Hadoop] → [安装Hive] → [安装数据生成器]
                                                    ↓
    [查看结果] ← [比较分析] ← [运行查询] ← [创建表] ← [生成数据]
```

## 测试查询说明

本项目包含 5 个 TPC-H 标准查询：

1. **Query 1** - 价格汇总报告
   - 测试点：简单聚合查询
   - 考察：基础查询性能和压缩效率

2. **Query 2** - 最小成本供应商
   - 测试点：复杂 JOIN 和子查询
   - 考察：多表关联性能

3. **Query 3** - 运输优先级
   - 测试点：多表 JOIN 和排序
   - 考察：JOIN 优化和排序性能

4. **Query 4** - 订单优先级检查
   - 测试点：EXISTS 子查询
   - 考察：子查询优化能力

5. **Query 5** - 本地供应商销量
   - 测试点：多表 JOIN 和聚合
   - 考察：复杂查询综合性能

## 预期结果示例

### 存储空间对比 (1GB 原始数据)

| 格式 | 存储大小 | 压缩比 | 相对大小 |
|------|----------|--------|----------|
| TextFile | ~1000 MB | 0% | 100% |
| RCFile | ~600 MB | 40% | 60% |
| Parquet | ~300 MB | 70% | 30% |
| ORC | ~250 MB | 75% | 25% |

### 查询性能对比

| 格式 | 平均查询时间 | 相对性能 |
|------|--------------|----------|
| TextFile | 基准 (100%) | 最慢 |
| RCFile | ~70% | 较快 |
| Parquet | ~40% | 快 |
| ORC | ~30% | 最快 |

## 项目结构

```
.
├── README.md                    # 英文说明文档
├── 快速开始.md                  # 本文件 (中文快速指南)
├── SETUP.md                     # 详细安装指南
├── TESTING_GUIDE.md             # 详细测试指南
├── setup/                       # 安装脚本
│   ├── 01-install-prerequisites.sh
│   ├── 02-install-hadoop.sh
│   ├── 03-install-hive.sh
│   └── 04-setup-testbench.sh
├── scripts/                     # 测试脚本
│   ├── generate-data.sh         # 生成测试数据
│   ├── create-tables.sh         # 创建不同格式的表
│   ├── run-queries.sh           # 执行测试查询
│   └── compare-results.sh       # 对比分析结果
├── queries/                     # SQL 查询文件
│   ├── query1.sql
│   ├── query2.sql
│   ├── query3.sql
│   ├── query4.sql
│   └── query5.sql
├── config/                      # 配置文件模板
│   ├── core-site.xml
│   ├── hdfs-site.xml
│   └── hive-site.xml
└── results/                     # 测试结果输出目录
```

## 常见问题

### Q1: SSH 连接失败

```bash
# 重新配置 SSH
ssh-keygen -t rsa -P "" -f ~/.ssh/id_rsa
cat ~/.ssh/id_rsa.pub >> ~/.ssh/authorized_keys
chmod 600 ~/.ssh/authorized_keys
```

### Q2: Hadoop 服务启动失败

```bash
# 检查服务状态
jps

# 如果服务缺失，尝试重启
stop-all.sh
start-all.sh
```

### Q3: Hive 无法连接

```bash
# 检查 HDFS 是否运行
hdfs dfs -ls /

# 重新创建 Hive 目录
hdfs dfs -mkdir -p /user/hive/warehouse
hdfs dfs -chmod g+w /user/hive/warehouse
```

### Q4: 内存不足

如果系统内存较小（少于 4GB），可以：
1. 使用更小的数据集（scale factor 0.1 或 0.5）
2. 调整 YARN 内存配置
3. 在 Hive 中使用本地模式

### Q5: 查询运行很慢

对于测试环境，可以在 Hive 中设置本地模式：

```sql
SET mapreduce.framework.name=local;
SET hive.exec.mode.local.auto=true;
```

## 实验报告建议内容

你的实验报告应该包含：

### 1. 环境配置 (10%)
- 系统信息和硬件配置
- Hadoop/Hive 版本和配置参数
- 遇到的问题和解决方法

### 2. 测试过程 (20%)
- 数据生成过程和参数
- 表创建过程
- 查询执行过程

### 3. 测试结果 (40%)
- 存储空间对比表和图表
- 查询性能对比表和图表
- 原始测试数据

### 4. 分析和讨论 (20%)
- 各存储格式的特点分析
- 性能差异的原因分析
- 使用场景建议

### 5. 总结 (10%)
- 测试结论
- 优化建议
- 心得体会

## Web 界面访问

测试过程中可以通过以下界面监控：

- **HDFS NameNode UI**: http://localhost:9870
- **YARN ResourceManager UI**: http://localhost:8088
- **MapReduce JobHistory**: http://localhost:19888

## 进阶选项

### 测试更大的数据集

```bash
bash generate-data.sh 10  # 10GB 数据
```

### 添加自定义查询

在 `queries/` 目录创建新的 SQL 文件，然后修改 `run-queries.sh`。

### 测试不同的压缩算法

```sql
SET hive.exec.compress.output=true;
SET mapreduce.output.fileoutputformat.compress.codec=org.apache.hadoop.io.compress.SnappyCodec;
```

## 获取帮助

- 详细安装步骤: 参见 `SETUP.md`
- 详细测试指南: 参见 `TESTING_GUIDE.md`
- 项目概述: 参见 `README.md`

## 参考资源

- Apache Hive 官方文档: https://hive.apache.org/
- TPC-H 基准测试: http://www.tpc.org/tpch/
- Hadoop 文档: https://hadoop.apache.org/
- ORC 文件格式: https://orc.apache.org/
- Parquet 文件格式: https://parquet.apache.org/

## 许可证

本项目为教育用途，用于大数据课程作业。
